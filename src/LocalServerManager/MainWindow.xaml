<Window x:Class="LocalServerManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:LocalServerManager.Views"
        mc:Ignorable="d"
        Title="Local Server Manager" 
        Height="700" 
        Width="1100"
        materialDesign:ThemeAssist.Theme="Dark"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid>
        <TabControl 
            materialDesign:ColorZoneAssist.Mode="PrimaryMid"
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch"
            Padding="16">
            
            <TabItem x:Name="ProjectsTab" Header="Проекты">
                <views:ProjectsView/>
            </TabItem>
            
            <TabItem x:Name="ServerTab" Header="Сервер">
                <views:ServerView/>
            </TabItem>
            
            <TabItem x:Name="DockerTab" Header="Docker">
                <views:DockerView/>
            </TabItem>
            
            <TabItem x:Name="SettingsTab" Header="Настройки">
                <views:SettingsView/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
