<Window x:Class="LocalServerManager.Views.DockerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewmodels="clr-namespace:LocalServerManager.ViewModels"
        mc:Ignorable="d" 
        d:DesignHeight="600" d:DesignWidth="1000"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и фильтры -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Docker Контейнеры" 
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       Foreground="{DynamicResource MaterialDesignBody}"
                       VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" Margin="16,0,0,0">
                <Button Command="{Binding RefreshContainersCommand}"
                        Content="Обновить" 
                        materialDesign:ButtonAssist.CornerRadius="4"
                        Padding="16,8"
                        Margin="0,0,8,0"/>
                <Button Command="{Binding StartContainerCommand}"
                        Content="Запустить" 
                        materialDesign:ButtonAssist.CornerRadius="4"
                        Padding="16,8"
                        Margin="0,0,8,0"/>
                <Button Command="{Binding StopContainerCommand}"
                        Content="Остановить" 
                        materialDesign:ButtonAssist.CornerRadius="4"
                        Padding="16,8"
                        Margin="0,0,8,0"/>
                <Button Command="{Binding RemoveContainerCommand}"
                        Content="Удалить" 
                        materialDesign:ButtonAssist.CornerRadius="4"
                        Padding="16,8"
                        Margin="0,0,8,0"/>
                <Button Command="{Binding ShowLogsCommand}"
                        Content="Логи" 
                        materialDesign:ButtonAssist.CornerRadius="4"
                        Padding="16,8"/>
            </StackPanel>
        </StackPanel>

        <!-- Список контейнеров -->
        <Border Grid.Row="1" 
                Background="{DynamicResource MaterialDesignCardBackground}"
                CornerRadius="4"
                Padding="16"
                Margin="0,0,0,16">
            <ListBox ItemsSource="{Binding Containers}"
                     SelectedItem="{Binding Selected}"
                     SelectionMode="Single">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                        <Setter Property="Padding" Value="12"/>
                        <Setter Property="Margin" Value="0,2"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="12" 
                                Background="{DynamicResource MaterialDesignPaper}"
                                CornerRadius="4"
                                Margin="0,2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Статус -->
                                <materialDesign:PackIcon Kind="Container" 
                                                         Width="32" Height="32"
                                                         Foreground="#FF2196F3"
                                                         VerticalAlignment="Center"/>

                                <!-- Информация -->
                                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                    <TextBlock Text="{Binding Name}" 
                                               Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                               Foreground="{DynamicResource MaterialDesignBody}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <materialDesign:Chip Content="{Binding State}" 
                                                             Background="{Binding StateColor}"
                                                             Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Image}" 
                                                   Style="{StaticResource MaterialDesignBody2TextBlock}"
                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Статус бар -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                    <TextBlock Text="{Binding State, StringFormat='Состояние: {0}'}" 
                                               Style="{StaticResource MaterialDesignBody2TextBlock}"
                                               Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Логи выбранного контейнера -->
        <TextBlock Grid.Row="2" 
                   Text="Логи контейнера" 
                   Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                   Foreground="{DynamicResource MaterialDesignBody}"
                   Margin="0,0,0,8"/>

        <Border Grid.Row="2" 
                Background="#FF1E1E1E"
                CornerRadius="4"
                Padding="16">
            <ScrollViewer materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Logs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" 
                                       FontFamily="Consolas"
                                       FontSize="12"
                                       Foreground="#FF90EE90"
                                       TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Статус бар -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    Margin="0,16,0,0">
            <materialDesign:PackIcon Kind="Docker" 
                                     Width="16" Height="16"
                                     Foreground="#FF2196F3"
                                     VerticalAlignment="Center"/>
            <TextBlock Text="{Binding DockerStatusText}" 
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       Margin="4,0,16,0"/>
            <TextBlock Text="{Binding Containers.Count, StringFormat='• {0} контейнера(ов)'}" 
                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>
    </Grid>
</Window>